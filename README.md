# CS499GameLauncher
# (can ignore) npm install -D tailwindcss@^3 postcss autoprefixer (version 4 was causing so many issues with tailwind, this line is just to remember the install syntax)
# install nodejs (v22.20.0)
# npm -v 11.6.1

############## to run after installing nodejs vvvvv #################
# cd to root folder (contains node_modules folder, public folder, src folder, package.json, etc)
# npm install (recreates node_modules, this is where ^^ the ignore might come in to play)
# npm run dev
# go to local host in web browser
